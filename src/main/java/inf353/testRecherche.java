package inf353;


public class testRecherche {
    public static void main(String[] args) {
     
       // DictionnaireHash dict2 = new DictionnaireHash(System.getProperty("user.dir"));
        Queries queries = new Queries(System.getProperty("user.dir") + "/inf353-tests");

        //Queries queries = new Queries(System.getProperty("user.dir") + "/inf353-tests");
        //Queries queries = new Queries(System.getProperty("user.dir") + "/inf353-tests");
    }
}